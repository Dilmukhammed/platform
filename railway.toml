# Railway deployment config
# Bun + Next.js — auto-detected via packageManager field in package.json

[build]
builder = "nixpacks"

[deploy]
startCommand = "bun run start"
healthcheckPath = "/api/v1/health"
